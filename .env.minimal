# Minimal configuration for Railway deployment without Twilio
# Copy this to .env for a basic deployment

# Required: JWT Secret Key (Railway can auto-generate this)
SECRET_KEY=your-secret-key-here-change-this-to-something-random

# Optional: Enable Claude AI chatbot (recommended)
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# That's it! The app will work without Twilio/WhatsApp integration
# Database will be auto-configured by Railway if you add PostgreSQL service